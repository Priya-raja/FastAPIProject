[project]
name = "fastapi-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
    "sqlalchemy",
    "pydantic",
    "timescaledb",
    "sqlmodel",
    "uvicorn>=0.38.0",
]
